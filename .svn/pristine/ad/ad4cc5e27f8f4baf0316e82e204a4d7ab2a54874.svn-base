#!/bin/bash

. ../set_si_env.sh $*

if [ ! "$IS_PROCEED" = "true" ]; then
	return
fi

"$DB2_HOME"/bin/db2 LOAD FROM "$FILE_PATH" OF DEL MODIFIED BY COLDEL, TIMESTAMPFORMAT=\"DDMMYYYY\" DUMPFILE="$DUMP_PATH" IMPLIEDDECIMAL USEDEFAULTS METHOD P \(1, 2, 3, 4, 5, 6, 7, 8\) MESSAGES "$LOG_PATH" REPLACE INTO $DB_SCHEMA.HP_TEMP_PF137\(RECORD_TYPE, INS_NUM, INS_NAME, INS_ADDR, INS_CITY_STATE, INS_CO_PHONE_NUM, RSV_PERC, PANEL_IND\) NONRECOVERABLE INDEXING MODE AUTOSELECT

 