#!/bin/bash

. ../set_si_env.sh $*

if [ ! "$IS_PROCEED" = "true" ]; then
	return
fi

"$DB2_HOME"/bin/db2 LOAD FROM "$FILE_PATH" OF ASC MODIFIED BY NOROWWARNINGS TIMESTAMPFORMAT=\"DDMMYYYY\" DUMPFILE="$DUMP_PATH" IMPLIEDDECIMAL STRIPTBLANKS NULLINDCHAR=Y METHOD L \(	1 1,	2 20,	21 24,	25 43\) NULL INDICATORS \(	0, 	0, 	0, 	0\) MESSAGES "$LOG_PATH" REPLACE INTO "$DB_SCHEMA".SI_TEMP_ARBS_CA002 \(	RECORD_TYPE,	CIF_ID,	CIF_SOURCE,	AA_NUMBER\) NONRECOVERABLE INDEXING MODE AUTOSELECT

