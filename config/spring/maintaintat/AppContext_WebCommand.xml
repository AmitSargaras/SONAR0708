<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xmlns:aop="http://www.springframework.org/schema/aop"
		xsi:schemaLocation="
			http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
			http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-2.5.xsd">
	
	<description>
		To be used for ICommand instances. 'id' naming recommended to be 'Action.Command'.
		All the ICommand instances will be injected into 'nameCommandMap' property of Action class.
		Injecting is done in 'action-[module]-servlet.xml' context file, resided in /WEB-INF/context folder.

		Avoid getting ICommand instance via BeanHouse or ApplicationContext.

		Inject proxy(s) into command class
	</description>
	
	<bean id="MaintainTATAction.SaveStageDurationCmd" class="com.integrosys.cms.ui.maintaintat.SaveStageDurationCmd">
		<property name="tatParamProxy" ref="tatParamProxy"/>
	</bean>
	<!-- 
	<bean id="TatDocAction.TatDocValidationCommand" class="com.integrosys.cms.ui.tatdoc.TatDocValidationCommand">
		<property name="checkListProxyManager" ref="checklistProxy"/>
		<property name="validDocumentStatusForDisbursementCompletionDate" value="${chklist.item.valid.states.for.completion.date.dist}"/>
		<property name="validDocumentStatusForPostDisbursementCompletionDate" value="${chklist.item.valid.states.for.completion.date.post.dist}"/>
		<property name="isFilterPreApprovalDocuments" value="${checklist.filter.pre.approval.docs}"/>
	</bean>
	 -->
</beans>