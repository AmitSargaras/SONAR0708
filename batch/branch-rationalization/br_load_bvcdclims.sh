#!/bin/bash

. set_env.sh $*

if [ ! "$IS_PROCEED" = "true" ]; then
	return
fi

"$DB2_HOME"/bin/db2 LOAD FROM "$FILE_PATH" OF ASC MODIFIED BY NOROWWARNINGS DUMPFILE="$DUMP_PATH" STRIPTBLANKS IMPLIEDDECIMAL NULLINDCHAR=Y METHOD L \(1 3, 4 6, 7 7, 8 26, 27 45, 46 64, 65 83\) NULL INDICATORS \(0, 0, 0, 0, 0, 0, 0\) MESSAGES "$LOG_PATH" REPLACE INTO "$DB_SCHEMA".BR_TEMP_BVCDCLIMS \(OBRN, NBRN, ATYP, OACCG, NACCG, OFDR, NFDR\) COPY NO INDEXING MODE AUTOSELECT
